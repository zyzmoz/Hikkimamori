program Hikkimamori;

uses
  Forms,
  untMain in 'untMain.pas' {Form1},
  bematech_imp in 'bematech_imp.pas',
  declaracoes in 'declaracoes.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
